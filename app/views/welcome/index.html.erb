<p style="text-align: center">Welcome to the <%= site_name %>, the book sharing community of <a href="http://www.amsterdam50.nl/">Amsterdam50</a> in Amsterdam (NL).</p>

<div class="two-columns">
	<div class="column"><div class="content">
		<h2>Recent additions</h2>
		<%= render :partial => 'items/item_blocks', :locals => { :items => @new_items.limit(9) } %>
	</div></div>

	<div class="column"><div class="content">
		<h2>Recent loans</h2>
		<%= render :partial => 'items/item_blocks', :locals => { :items => @new_loans.limit(9) } %>
	</div></div>
</div>

<% if current_user %>
	<p>We have <%= User.count %> members: <%= User.order('activated_at DESC').map{|u| user_link(u)}.join(", ").html_safe %>.</p>
<% end %>

